# Task 1.7 — Define decoder output contract for reconstruction compatibility [#R7]

This validation bundle verifies that:

- The decode output contract requires `xyz` and `atomic_nums`
- `aromatic` and `atom_affinity` are treated as optional fields
- The reconstruction entry function exists and matches the documented contract:
  `utils.reconstruct.reconstruct_from_generated`

Run via `run.sh` (macOS/Linux) or `run.bat` (Windows).

